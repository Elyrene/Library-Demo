<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.Library.mapper.ReaderInfoMapper">
    <select id="findByUsername" resultType="com.Library.entity.ReaderInfo">
        SELECT id, username, password, realName, sex, birthday, address, tel, email, registerDate, readerNumber
        FROM reader_info
        WHERE username = #{username}
        LIMIT 1
    </select>
</mapper>